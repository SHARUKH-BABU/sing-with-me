<!doctype html>
<html class="scroll-smooth">
<head>
  <meta charset="UTF-8">
  <title>LOGIN</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-violet-200 flex flex-col justify-center items-center h-screen">
  <% if(err_msg) { %>
      <div id="error-msg" style=" text-align: center; border-radius: 10px; padding:10px; color: white; background-color: rgb(233, 69, 69);" class="w-[10%] sm:w-[80%]">
              <i class="fa-solid fa-circle-x"></i>
              <%= err_msg %>
       </div>
  <% } %>
  <h1 class="text-center text-violet-700 drop-shadow-lg text-xl font-bold">LOGIN</h1>   
  <div class="container m-3 mx-auto min-w-60">
    <form action="/login" method="post" class=" shadow-2xl rounded-md border-violet-500 border-2 p-4 py-8 bg-violet-600 flex justify-center flex-col gap-2 max-w-md mx-auto">
      <div class="form-inputs flex flex-col gap-5 justify-center items-center p-4 rounded-md">
        <div class="input-block">
          <label for="uname" class="text-white after:content-['*'] after:ml-0.5 after:text-red-500 text-md font-medium text-slate-700">
            USER-ID 
          </label>
          <input type="text" name="uname"  autocomplete="off" id="uname" required class="mx-2 md:mx-1 focus:outline-none rounded-md px-2 py-1">
        </div>
        <div class="input-block">
          <label for="upassword" class="text-white after:content-['*'] after:ml-0.5 after:text-red-500 text-md font-medium text-slate-700">
            PASSWORD 
          </label>
          <input type="password" autocomplete="off" name="upassword" id="upassword"  required class="mx-2 md:mx-1 focus:outline-none rounded-md px-2 py-1">
        </div>
      </div>
      <div class="submitting flex justify-between p-5">
        <p class="text-gray-300">dont have account <span class="text-red-500 ">?</span><br><a href="signup" class="text-gray-200 font-bold underline">Sign-In</a></p>
        <button type="submit" class="mx-auto bg-gradient-to-r from-green-400 to-blue-500 hover:from-pink-500 hover:to-yellow-500 p-2 rounded-md w-[50%] ">
          LOGIN
        </button>
      </div>
    </form>
  </div>

  <script>
        setTimeout(function() {
                var errorMsg = document.getElementById('error-msg');
                if (errorMsg) {
                    errorMsg.parentNode.removeChild(errorMsg);
                }
                
            }, 3000);
    </script>
</body>
</html>
